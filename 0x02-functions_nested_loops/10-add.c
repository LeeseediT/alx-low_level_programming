#include "main.h"
/**
 * add - adds two integers
 * @a: one of the inetgers being added
 * @b: one of the inetgers being added
 * Return: result of addition
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
